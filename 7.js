// Comparsions
// Stejné jako v pythonu až na jednu výjimku: === vs ==
    // Strict Equality (===;!==) - Neprovádí se převod typů a hodnoty různých typů jsou vždy rozdílné 
        // '3' === 3 (false)
        // Vždy používat
    // Loose equality - Provádí se převod typů před porovnáním
        // 0 == false (true)
        // Nepoužívat

// String comparsion
//'Apple' > 'Pear'; => false (co je dříve v abecedě má "menší" hodnotu)
//'a' < 'above'; => true
// '10' < '2'; => true (jelikož 1 je před 2)
